const conf = {
    /*
    TODO implement configurable timer using range
    // range extremities + default value
    min: 5,
    max: 120,
    default: 30,
    */

    // update frequency (ms)
    delay: 100,

    // circle conf
    ox: 50,
    oy: 50,
    r: 50,

    // default countdown (minutes)
    minutes: 30,

};
