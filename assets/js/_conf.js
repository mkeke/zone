const conf = {

    // range extremities + default value
    min: 1,
    max: 120,
    default: 30,

    // update frequency (ms)
    delay: 100,

    // circle conf
    ox: 50,
    oy: 50,
    r: 50,

};
